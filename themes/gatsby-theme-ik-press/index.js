// boop
// ./package.json in its "main" field refers to this fole - so it needs to exist
// cite: Since themes also function as plugins, this is an empty file that Gatsby requires in order to use this theme as a plugin.
// alternatively "main" cna refer to another existing file - "gatsby-config.js" for example
